server:
  port: 8081

security:
  jwt:
    secret: VERY_SECRET_KEY_CHANGE_ME_123456789
    expiration: 3600000   # 1 hour (milliseconds)
    
spring:

  datasource:
    url: jdbc:mysql://localhost:3306/auth_db  #127.0.0.1 = localhost
    username: root
    password: Ahmed@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
